apiVersion: v1
kind: ConfigMap
namespace: kube-system
metadata:
  name: gpu-resource-config
data:
  any: |
    version: 1
    sharing:
      timeSlicing:
        resources:
        - name: nvidia.com/gpu
          replicas: 4
