# Private experiment artifacts and local topology configuration
.env*
/artifacts/private/
/artifacts/staging/

# Historical M0 private evidence path; keep the legacy workflow protected
/artifacts/m0-private/
deployments/bootstrap/out/
deployments/bootstrap/platform-qualification/cuda-smoke/vector_add

docs/environment/*.private.md
helper/
.codex/

# Local-only working notes (drafts, job search) — must never reach the public repo
*.private.md

# Interrupted publisher scratch/backup directories (staging may be unsanitized)
benchmarks/raw-results/m0-platform-qualification/.*.staging.*/
benchmarks/raw-results/m0-platform-qualification/.*.previous.*/

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
